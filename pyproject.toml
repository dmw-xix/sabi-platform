[project]
name = "sabi-platform"
version = "0.1.0"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["config*", "database*", "scrapers*", "intelligence*"]

[tool.pytest.ini_options]
pythonpath = ["."]